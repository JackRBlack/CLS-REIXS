This is a static 32bit binary which should run under all Linuxes
(thus the executable is "so big").

If you compile it yourself (recommended approach!) by command
	make g++
then the executable will take less than 50 KiB and 32bit or 64bit
according to your platform.

